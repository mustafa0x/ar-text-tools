{
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "cssnano": "^5.1.0",
    "glob": "^7.2.0",
    "postcss": "^8.4.7",
    "terser": "^5.12.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && node build.js"
  },
  "type": "module"
}
